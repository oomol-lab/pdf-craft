# 2.2.3

- Removed the legacy callback-based PDF translation API. Translate a PCEX with
  a structured chapter transformer, then patch that translated extraction back
  into the source PDF.
- Text fragments inside one PCEX TextFlowItem now share a read-only logical
  text projection across Narrative translation, PDF patching, Markdown, EPUB,
  and anchored-content context generation.
